name: "Tester 🔧"

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: 🔧 Build
        uses: actions/setup-node@v2.1.4
          with:
              node-version: 12
              registry-url: https://registry.npmjs.org
        run:
          npm config set ignore-scripts false && npm i
      - name: 🟢 Test
        run: npm run test:unit
