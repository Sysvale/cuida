import { z } from 'zod';

export const ColorsPalleteInputSchema = z.object({});

export const TypographyInputSchema = z.object({});

export const FontWeightInputSchema = z.object({});

export const SpacingInputSchema = z.object({});

export const BordersInputSchema = z.object({});

export const ElevationInputSchema = z.object({});

export const ShadowsInputSchema = z.object({});

export const TransitionsInputSchema = z.object({});

export const IconographyInputSchema = z.object({});
