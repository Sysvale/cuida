import { Meta, Story, Props, ArgsTable, Canvas, Preview } from '@storybook/addon-docs/blocks';
import ProgressBar from '../components/ProgressBar.vue';

<Meta
  title="ProgressBar"
  component={ ProgressBar }
  argTypes={{
    color: { control: 'color' },
    backgroundColor: { control: 'color' },
    percentage: {
      description: 'Prop teste',
      control: {
        type: 'range',
        min: 0,
        max: 1,
        step: 0.01,
      },
    },
  }
}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { ProgressBar },
  template:`<progress-bar v-bind="$props"/>`,
});

# ProgressBar

##### Progress bars are components used to indicate the status of something to the user

<Canvas>
  <Story
    name="Progress bar"
    args={{
      percentage: 0.5,
      backgroundColor: '#DDE2E7',
      color: '#2EB88D',
    }}
  >
    { Template.bind({}) }
  </Story>
</Canvas>

<ArgsTable story="Progress bar" />