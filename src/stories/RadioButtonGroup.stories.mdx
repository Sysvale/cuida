import { Meta, Story, Props, ArgsTable, Canvas, Preview } from '@storybook/addon-docs/blocks';
import RadioButtonGroup from '../components/RadioButtonGroup.vue';

<Meta
	title="Componentes/RadioButtonGroup"
	component={ RadioButtonGroup }
/>

export const Template = (args, { argTypes }) => ({
	props: Object.keys(argTypes),
	components: { RadioButtonGroup },
	template: /*html*/ `
		<radio-button-group
			v-bind="$props" 
		/>
	`,
});

# RadioButtonGroup

### RadioButtonsGroup são radio buttons customizados para facilitar a seleção e feedback
---
<br />

## Recomendamos o uso de RadioButtonGroups quando:
- As opções você quer prover com o componente são mutuamente exclusivas.
- Você precisar utilizar um ```<input type="radio" />```.

<br />

## Não recomendamos o uso de RadioButtonGroups quando:
- Duas ou mais opções puderem ser selecionadas ao mesmo tempo.
- Quando você tiver 4 ou mais opções. Prefira Dropdowns nesses casos.

## Obs.:
- Você pode disabilitar cada um dos radio buttons passando a propriedade ```disable: true```, no respectivo objeto
no array de opções. Caso você queira disabilitar todo o grupo, você pode setar a prop ```disabled``` como true no componente.

<br />

## Preview

export const value = '';
export const options = [
	{
		text: 'Opção desabilitada',
		value: 'value1',
		disabled: true,
	},
	{
		text: 'Opção habilitada 1',
		value: 'value2',
	},
	{
		text: 'Opção habilitada 2',
		value: 'value3',
	},
];

<Canvas>
	<Story
		name="Radio button group"
		args={{
			options,
			value,
			disabled: false,
			inline: false,
		}}
	>
		{ Template.bind({}) }
	</Story>
</Canvas>

<ArgsTable story="Radio button group" />