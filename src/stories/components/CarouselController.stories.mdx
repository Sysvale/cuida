import { Meta, Story, Props, ArgsTable, Canvas, Preview } from '@storybook/addon-docs/blocks';
import CarouselController from '../../components/CarouselController.vue';

<Meta
	title="Componentes/Containers/CarouselController"
	component={ CarouselController }
	argTypes={{
		variant: {
			control:{
				type: 'select',
				options: [
					'info',
					'success',
					'warning',
					'danger',
				],
			}
		},
	}
}
/>

export const Template = (args, { argTypes }) => ({
	props: Object.keys(argTypes),
	components: { CarouselController },
	template: /*html*/ `
		<cds-carousel-controller
			@click-forward="handleClickForwardEvent"
			@click-back="handleClickBackEvent"
			v-bind="$props" 
		>
			Texto do alerta pelo slot
		</cds-carousel-controller>
	`,
	methods: { 
		handleClickForwardEvent() { console.log('clicked forward'); },
		handleClickBackEvent() { console.log('clicked back'); }
	},
});

export const template = /*html*/ `
	<cds-carousel-controller
		variant="info"
		text="Texto do alerta"
	/>`;

export const docsDecorator = () => {
	return {
		data() {
			return {
				template,
			};
		},
		template: /*html*/ `
			<source-code-wrapper
				:exampleSourceCode="template"
			>
				<story slot="component-preview"/>
			</source-code-wrapper>`,
	};
};

# CarouselController

### CarouselControllers são componentes utilizados para controlar a exibição de elementos em carrosséis.
---
<br />

## Recomendamos o uso de CarouselControllers quando:
- Você possuir uma categoria de elementos da interface que se repetem e que podem ser exibidos em forma de carrossel.

<br />

## Não recomendamos o uso de CarouselControllers quando:
- Você estiver trabalhando com tabelas. Nesses casos recomendamos o uso de paginação (TODO).
- Todo o conteúdo a ser gerenciado puder ser exibido de uma só vez. Nesse caso não há necessidade de controle de páginas e do uso do componente;

<br />

## Preview

<Canvas>
	<Story
		name="CarouselController"
		decorators={[docsDecorator]}
		args={{
			total: 12,
			perPage: 4,
			propertyName: 'Métricas',
		}}
	>
		{ Template.bind({}) }
	</Story>
</Canvas>

<ArgsTable story="CarouselController" />