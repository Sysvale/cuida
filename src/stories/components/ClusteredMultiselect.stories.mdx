import { Meta, Story, Props, ArgsTable, Canvas, Preview } from '@storybook/addon-docs/blocks';
import ClusteredMultiselect from '../../components/ClusteredMultiselect.vue';

<Meta
	title="Componentes/Forms/ClusteredMultiselect"
	component={ ClusteredMultiselect }
	argTypes={{
		options: {
			description: 'Lista de opções do ClusteredMultiselect',
		},
		value: {
			description: 'Lista de itens selecionados do ClusteredMultiselect',
		},
		label: {
			description: 'Propriedade do objeto options, que vai constar nas tags listadas no ClusteredMultiselect',
		},
		trackBy: {
			description: 'Utilizada para comparar objetos.',
		},
	}
}
/>

export const Template = (args, { argTypes }) => ({
	props: Object.keys(argTypes),
	components: { ClusteredMultiselect },
	template: /*html*/ `
		<clustered-multiselect
			v-bind="$props" 
		/>
	`,
});

export const template = /*html*/ `
	<cds-clustered-multiselect
		v-model="filteredOptions"
		label="title"
		track-by="title"
		:options="options"
	/>`;

export const docsDecorator = () => {
	return {
		data() {
			return {
				template,
			};
		},
		template: /*html*/ `
			<source-code-wrapper
				:exampleSourceCode="template"
			>
				<story slot="component-preview"/>
			</source-code-wrapper>`,
	};
};

# ClusteredMultiselect

### ClusteredMultiselects são componentes utilizados para criar soluções de navegação e menus.
---
<br />

## Recomendamos o uso de ClusteredMultiselects quando:
- Houver necessidade de separar o conteúdo por páginas e navegar por elas.
- Não for o caso de utilizar soluções como tabs, sidebars, cards ou links como navegação.

<br />

## Não recomendamos o uso de ClusteredMultiselects quando:
- Houver muitos itens a serem mostrados no menu.
- Houver a necessidade de exibir links verticalmente.

## Observações:
- O ClusteredMultiselect do Cuida é um wrapper do <a href="https://vue-multiselect.js.org/">Vue Multiselect</a>
com a sua adequação aos nossos princípios e recomendações. Para informações detalhadas sobre o componente, recomendamos
leitura da documentação.

<br />

## Preview

export const options = [
	{ title: 'Avengers',},
	{ title: 'Naruto',},
	{ title: 'X-men',},
	{ title: 'The Power-puff Girls',}
];

export const value = [
	{ title: 'Avengers',},
	{ title: 'The Power-puff Girls',}
];

<Canvas>
	<Story
		name="ClusteredMultiselect"
		decorators={[docsDecorator]}
		args={{
			options,
			value,
			label: 'title',
			trackBy: 'title',
		}}
	>
		{ Template.bind({}) }
	</Story>
</Canvas>

<ArgsTable story="ClusteredMultiselect" />
