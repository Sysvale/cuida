import { Meta, Story, Props, ArgsTable, Canvas, Preview } from '@storybook/addon-docs/blocks';
import Divider from '../../components/Divider.vue';

<Meta
	title="Componentes/Containers/Divider"
	component={ Divider }
	argTypes={{
		variant: {
			control:{
				type: 'select',
				options: [
					'info',
					'success',
					'danger',
				],
			}
		},
	}
}
/>

export const Template = (args, { argTypes }) => ({
	props: Object.keys(argTypes),
	components: { Divider },
	template: /*html*/ `
		<divider v-bind="$props">
			<p>Lorem Ipsum</p>
			<p>Lorem Ipsum</p>
			<p>Lorem Ipsum</p>
			<p>Lorem Ipsum</p>
		</divider>
	`,
});

export const template = /*html*/ `
	<cds-divider
		animated
		:duration="1"
		:delay="0"
	>
		<span> Texto bem importante</span>
	</cds-divider>`;

export const docsDecorator = () => {
	return {
		data() {
			return {
				template,
			};
		},
		template: /*html*/ `
			<source-code-wrapper
				:exampleSourceCode="template"
			>
				<story slot="component-preview"/>
			</source-code-wrapper>`,
	};
};

# Divider

### Divider são componentes utilizados destacar palavras ou frases importantes em um determinado contexto.
---
<br />

## Recomendamos o uso de Dividers quando:
- Você deseja guiar a atenção do usuário para palavras ou frases espefícias na interface (nesses cenários Dividers animados podem ser ainda mais efetivos que os comuns)
- Você está implementando alguma feature de <i>diff</i> e precisa indicar elementos que foram substituídos por outros

<br />

## Não recomendamos o uso de Dividers quando:
- A palavra ou sentença se repetir na interface como em títulos de card, headers, etc. Para esses casos, use mecanismos de hierarquia tipográfica como peso da fonte, tamaho ou cor
- O elemento sobre do qual o Divider será <i>wrapper</i> não for um elemento de texto.
- Forem destacados trechos muito grandes de texto. Isso pode acabar resultando no oposto do que se espera com o uso de Dividers 

<br />

## Preview

<Canvas>
	<Story
		name="Divider"
		decorators={[docsDecorator]}
		args={{}}
	>
		{ Template.bind({}) }
	</Story>
</Canvas>

<ArgsTable story="Divider" />