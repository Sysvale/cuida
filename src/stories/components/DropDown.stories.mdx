import { Meta, Story, Props, ArgsTable, Canvas, Preview } from '@storybook/addon-docs/blocks';
import DropDown from '../../components/DropDown.vue';

<Meta
	title="Componentes/Forms/DropDown"
	component={ DropDown }
	argTypes={{
		options: {
			description: 'Lista de opções do DropDown',
		},
		value: {
			description: 'Lista de itens selecionados do DropDown',
		},
		label: {
			description: 'Propriedade do objeto options, que vai constar nas tags listadas no DropDown',
		},
		trackBy: {
			description: 'Utilizada para comparar objetos.',
		},
	}
}
/>

export const Template = (args, { argTypes }) => ({
	props: Object.keys(argTypes),
	components: { DropDown },
	template: /*html*/ `
		<DropDown
			v-bind="$props" 
		/>
	`,
});

export const template = /*html*/ `
	<cds-dropdown
		v-model="filteredOptions"
		label="title"
		track-by="title"
		:options="options"
	/>`;

export const docsDecorator = () => {
	return {
		data() {
			return {
				template,
			};
		},
		template: /*html*/ `
			<source-code-wrapper
				:exampleSourceCode="template"
			>
				<story slot="component-preview"/>
			</source-code-wrapper>`,
	};
};

# DropDown

### DropDowns são componentes utilizados para criar soluções de navegação e menus.
---
<br />

## Recomendamos o uso de DropDowns quando:
- For preciso separar conteúdo por páginas e navergar por elas.
- Não for o caso de utilizar soluções como tabs, sidebars, cards ou links como navegação.

<br />

## Não recomendamos o uso de DropDowns quando:
- Há muitos itens a serem mostrados no menu.
- For necessário mostrar os links verticalmente.

## Observações:
- O Dropdown do Cuida é um wrapper do <a href="https://vue-multiselect.js.org/">Vue Multiselect</a>
com a sua adequação aos nossos princípios e recomendações. Para informações detalhadas sobre o componente, recomendamos
leitura da documentação.

<br />

## Preview

export const options = [
	{ title: 'Avengers',},
	{ title: 'Naruto',},
	{ title: 'X-men',},
	{ title: 'The Powerpuff Girls',}
];

export const value = [
	{ title: 'Avengers',},
	{ title: 'The Powerpuff Girls',}
];

<Canvas>
	<Story
		name="DropDown"
		decorators={[docsDecorator]}
		args={{
			options,
			value,
			label: 'title',
			trackBy: 'title',
		}}
	>
		{ Template.bind({}) }
	</Story>
</Canvas>

<ArgsTable story="DropDown" />