import { Meta, Story, Props, ArgsTable, Canvas, Preview } from '@storybook/addon-docs/blocks';
import FilterPill from '../../components/FilterPill.vue';

<Meta
	title="Componentes/Display/FilterPill"
	component={ FilterPill }
/>

export const Template = (args, { argTypes }) => ({
	data() {
		return {
			isActive: false,
		};
	},
	props: Object.keys(argTypes),
	components: { FilterPill },
	template: /*html*/ `
		<filter-pill
			v-bind="$props"
			@click="isActive = !isActive"
		>
			<p>Slot do dropdown do FilterPill</p>
		</filter-pill>
	`,
});

export const template = /*html*/ `
	<cds-filter-pill 
		label="Idade"
		content="0 a 120 anos"
		@click="showSortingOptions"
	/>`;

export const docsDecorator = () => {
	return {
		data() {
			return {
				template,
			};
		},
		template: /*html*/ `
			<source-code-wrapper
				:exampleSourceCode="template"
			>
				<story slot="component-preview"/>
			</source-code-wrapper>`,
	};
};

# FilterPill

### FilterPills são componentes utilizados para ativar popovers contendo filtros para listas e views.
---
<br />

## Recomendamos o uso de FilterPills quando:
- Estiver trabalhando com filtros.
- Existirem popovers contendo opções de filtragem.

<br />

## Não recomendamos o uso de FilterPills quando:
- For necessário o uso de um botão.
- For necessário o uso de um dropdown.
- Estiver trabalhando com tabelas.

<br />

## Preview

<Canvas>
	<Story
		name="FilterPill"
		decorators={[docsDecorator]}
		args={{
			label: 'Idade',
			content: '0 a 120 anos',
		}}
	>
		{ Template.bind({}) }
	</Story>
</Canvas>

<ArgsTable story="FilterPill" />
