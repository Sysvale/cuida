import { Meta, Story, Props, ArgsTable, Canvas, Preview } from '@storybook/addon-docs';
import GridItem from '../../components/GridItem.vue';

<Meta
	title="Componentes/Containers/GridItem"
	component={ GridItem }
	parameters={{
		viewMode: 'docs',
		previewTabs: { canvas: { hidden: true }},
		docs: {
			source: {
				language: 'html',
				format:true,
				code: /*html*/ 
`<cds-grid-item
				subGrid
				gap="32px"
				colSpan="4"
			>
				<cds-grid
					gap="32px"
					cols="minmax(250px, auto) 1fr"
				>
					<div style="font-size: 18px; font-weight: 550; background-color: #E7EDF3; padding: 12px; border-radius: 12px; display: flex; align-items: center; justify-content: center">
						Cell 4
					</div>
					<div style="font-size: 18px; font-weight: 550; background-color: #E7EDF3; padding: 12px; border-radius: 12px; display: flex; align-items: center; justify-content: center">
						Cell 5
					</div>
				</cds-grid>
				<div style="font-size: 18px; font-weight: 550; background-color: #E7EDF3; padding: 12px; border-radius: 12px; display: flex; align-items: center; justify-content: center">
					Cell 6
				</div>
			</cds-grid-item>`
			},
		}
	}}
/>

export const Template = (args) => ({
	components: { CdsGridItem: GridItem },
	setup() {
		return { args };
	},
	template: /*html*/ `
		<cds-grid-item
			v-bind="args" 
		>
		</cds-grid-item>
	`,
});

# GridItem


<Story
	name="GridItem"
>
	{ Template.bind({}) }
</Story>

<ArgsTable story="GridItem" />
