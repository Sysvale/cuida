import { Meta, Story, Props, ArgsTable, Canvas, Preview } from '@storybook/addon-docs/blocks';
import ToggleSwitch from '../../components/ToggleSwitch.vue';

<Meta
	title="Componentes/Forms/ToggleSwitch"
	component={ ToggleSwitch }
/>

export const Template = (args, { argTypes }) => ({
	data() {
		return {
			isSelected: false,
		};
	},
	props: Object.keys(argTypes),
	components: { ToggleSwitch },
	template: /*html*/ `
		<toggle-switch
			v-model="isSelected"
			v-bind="$props"
		/>
	`,
});

export const template = /*html*/ `
	<cds-toggle-switch
		v-model="value"
	/>`;

export const docsDecorator = () => {
	return {
		data() {
			return {
				template,
			};
		},
		template: /*html*/ `
			<source-code-wrapper
				:exampleSourceCode="template"
			>
				<story slot="component-preview"/>
			</source-code-wrapper>`,
	};
};

# Switch

### Switchs são componentes utilizados para ativar popovers contendo filtros para listas e views.
---
<br />

## Recomendamos o uso de Switchs quando:
- Você estiver trabalhando com filtros.
- Recomendamos seu uso em conjunto com popovers contendo opções de filtragem.

<br />

## Não recomendamos o uso de Switchs quando:
- Você estiver procurando por um botão.
- Você estiver procurando um dropdown.
- Você estiver trabalhando com tabelas.

<br />

## Preview

<Canvas>
	<Story
		name="Switch"
		decorators={[docsDecorator]}
		args={{
			value: false,
		}}
	>
		{ Template.bind({}) }
	</Story>
</Canvas>

<ArgsTable story="Switch" />
